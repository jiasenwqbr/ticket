"use client";
interface Props {
  id?: string;
  avatar?: string;
  userId?: string;
  content?: string;
  images?: string[];
  options: Array<{ key: string; value: number }>;
}
function TicketTopic(props: Props) {
  return <></>;
}
export default TicketTopic;
